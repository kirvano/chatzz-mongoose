{
  "name": "mongoose",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "generate:types": "rm models/*.d.ts && tsc --declaration --allowJs --emitDeclarationOnly --outDir models"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongoose": "^8.1.3"
  },
  "devDependencies": {
    "@types/mongoose": "^5.11.97",
    "typescript": "^5.3.3"
  }
}
