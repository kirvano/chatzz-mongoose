{
  "name": "mongoose",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "generate:types": "rm -f models/*.d.ts && tsc --declaration --allowJs --emitDeclarationOnly --outDir models"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/mongoose": "^5.11.97",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "mongoose": "^6.8.4"
  }
}
